name: Tomato Crop ML - CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci-cd-pipeline:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      # ... rest stays the same          # ✅ updated from v3

      - name: Set up Python 3.10
        uses: actions/setup-python@v5      # ✅ updated from v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Lint with flake8
        run: |
          flake8 src/ --max-line-length=100 --ignore=E402,W503,E501

      - name: Train model
        run: python src/train.py

      - name: Run tests
        run: pytest tests/ -v

      - name: Evaluate model
        run: python src/evaluate.py

      - name: Upload model artifacts
        uses: actions/upload-artifact@v4   # ✅ already updated
        with:
          name: tomato-model-artifacts
          path: outputs/

      - name: Deploy to Hugging Face
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_REPO_ID: ${{ secrets.HF_REPO_ID }}
        run: python src/deploy.py